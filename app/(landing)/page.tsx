import {HeroSection} from "@/components/landing/hero-section";

export default async function Home() {
    return (
        <div className={""}>
            test
            <HeroSection/>
        </div>
    );
}
